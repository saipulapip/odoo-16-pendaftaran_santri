<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
        <!-- cep.kotak.apip tree view -->
        <record id="cep_kotak_apip_view_tree" model="ir.ui.view">
            <field name="name">cep.kotak.apip.view.tree</field>
            <field name="model">cep.kotak.apip</field>
            <field name="arch" type="xml">
                <tree>
                    <!-- Add your fields here -->
                    <field name="name"/>
                    <field name="description"/>
                </tree>
            </field>
        </record>
        <!-- cep.kotak.apip form view -->
        <record id="cep_kotak_apip_view_form" model="ir.ui.view">
            <field name="name">cep.kotak.apip.view.form</field>
            <field name="model">cep.kotak.apip</field>
            <field name="arch" type="xml">
                <form string="">
                    <sheet>
                        <group>
                            <!-- Add your fields here -->
                            <field name="name"/>
                            <field name="description"/>
                            <field name="user_id"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        <!-- cep.kotak.apip action window -->
        <record id="cep_kotak_apip_action" model="ir.actions.act_window">
            <field name="name">cep.kotak.apip.action</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">cep.kotak.apip</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    <!-- Add Text Here -->
                </p><p>
                    <!-- More details about what a user can do with this object will be OK --> 
                
                </p>
            </field>
        </record>
        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="cep_kotak_apip_menu_action" name="Kotak Apip" parent="setting_menu_categ" action="cep_kotak_apip_action" sequence="10"/>
    </data>

</odoo>
    
        